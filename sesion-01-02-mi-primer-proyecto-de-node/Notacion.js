//; -> sirve para delimitar una linea. se recomienda utilizarlo siempre.
const b = 4;
b + 4;
console.log('b', b)

// . -> se utilizan en los objetos para acceder a los atributos.
 
// []-> Listas, arreglos o arrays.
const ar = [1,2,3,4];
console.log('ar', ar)
//()->funciones
function suma(a, b) {
    //se escribe la función
}
//{} -> llaves para objetos, funciones y estructuras de control
const movil = {
    anchura: 5,
    altura: 10
}
 //o
 if(true) {
    //todo lo que haya estre llaves
 }